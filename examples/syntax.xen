include io;
include os;
include net;

fn control_flow() {
    var bool = true;
    if (bool) {
        // do something
    } else if () {
        // do something else
    } else {
        // do something else
    }

    var bool_2 = false;
    if (bool and bool_2) {}

    if (bool or bool_2) {}
}

fn operators() {}

fn loops() {}

fn lambdas() {}

fn classes() {}
